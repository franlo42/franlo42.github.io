---
interface Props {
	date: Date;
	clase: string
}

const { date,clase } = Astro.props;
---

<time  class={clase} datetime={date.toISOString()}>
	{
		date.toLocaleDateString('en-GB', {
			year: 'numeric',
			month: 'short',
			day: 'numeric',
		})
	}
</time>